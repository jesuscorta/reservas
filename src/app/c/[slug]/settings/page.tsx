import { ClubShell } from "@/components/club-shell";
import { Plus } from "lucide-react";

export default async function SettingsPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  return <ClubShell slug={slug} active="settings"><div className="mx-auto max-w-4xl p-5 md:p-8"><h1 className="text-2xl font-bold">Ajustes</h1><p className="mt-1 text-slate-600">Configura solo lo necesario para gestionar las reservas.</p><section className="mt-7 rounded-xl bg-white p-5 shadow-sm"><div className="flex items-center justify-between gap-3"><div><h2 className="font-bold">Pistas</h2><p className="text-sm text-slate-600">Las pistas desactivadas conservan su histórico.</p></div><button className="flex items-center gap-1 rounded-lg bg-emerald-700 px-3 py-2 text-sm font-bold text-white"><Plus size={17}/>Añadir pista</button></div><div className="mt-5 divide-y">{["Pista 1", "Pista 2", "Pista 3", "Pista 4"].map((court, index) => <div key={court} className="flex items-center justify-between py-3"><span className="font-medium">{index + 1}. {court}</span><button className="rounded-md px-3 py-1 text-sm font-semibold text-emerald-700">Editar</button></div>)}</div></section><section className="mt-5 rounded-xl bg-white p-5 shadow-sm"><h2 className="font-bold">Horario habitual</h2><p className="mt-1 text-sm text-slate-600">Los horarios especiales tienen prioridad.</p><div className="mt-4 grid gap-2 sm:grid-cols-2">{["Lunes", "Martes", "Miércoles", "Jueves", "Viernes", "Sábado", "Domingo"].map((day) => <div key={day} className="flex items-center justify-between rounded-lg bg-slate-50 px-3 py-3"><span className="font-medium">{day}</span><span className="text-sm">09:00 - 23:00</span></div>)}</div></section><section className="mt-5 rounded-xl bg-white p-5 shadow-sm"><h2 className="font-bold">Reservas</h2><label className="mt-4 flex cursor-pointer items-center justify-between gap-4 rounded-lg border p-4"><span><span className="block font-medium">Habilitar control de pagado</span><span className="block text-sm text-slate-600">Muestra Pendiente o Pagado en las reservas.</span></span><input type="checkbox" className="h-5 w-5 accent-emerald-700" /></label><label className="mt-4 block font-medium">Duración de cada turno<select className="mt-1 block rounded-lg border p-2"><option>90 minutos</option></select></label></section></div></ClubShell>;
}
