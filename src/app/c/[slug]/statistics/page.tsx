import { ClubShell } from "@/components/club-shell";

const stats = [["Ocupación general", "68%", "Esta semana"], ["Reservas", "42", "Esta semana"], ["Horas ocupadas", "63 h", "Esta semana"], ["Cancelaciones", "3", "Esta semana"]];
export default async function StatisticsPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; return <ClubShell slug={slug} active="statistics"><div className="p-5 md:p-8"><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-2xl font-bold">Estadísticas</h1><p className="text-slate-600">Una lectura rápida de la actividad del club.</p></div><select aria-label="Periodo" className="rounded-lg border bg-white p-2"><option>Esta semana</option><option>Este mes</option></select></div><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([title, value, detail]) => <article key={title} className="rounded-xl bg-white p-5 shadow-sm"><p className="text-sm font-medium text-slate-600">{title}</p><p className="mt-2 text-3xl font-bold">{value}</p><p className="mt-1 text-sm text-slate-500">{detail}</p></article>)}</div><div className="mt-6 grid gap-4 lg:grid-cols-2"><article className="rounded-xl bg-white p-5 shadow-sm"><h2 className="font-bold">Horas más concurridas</h2><p className="mt-4 text-2xl font-bold text-emerald-700">18:00 - 21:00</p></article><article className="rounded-xl bg-white p-5 shadow-sm"><h2 className="font-bold">Uso por pista</h2><div className="mt-4 space-y-3">{["Pista 1 · 84%", "Pista 2 · 76%", "Pista 3 · 62%"].map((item) => <div key={item} className="border-b pb-2 text-sm font-medium">{item}</div>)}</div></article></div></div></ClubShell>; }
